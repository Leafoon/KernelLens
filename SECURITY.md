# Security

KernelLens 将任务、所读取的材料及工具反馈发给用户配置的模型供应商。请使用适合处理这些材料的账户和服务，并选择合适的任务工作区。

文件工具包含路径、敏感文件、符号链接和写入校验；这些检查不是抵御恶意本机进程的操作系统沙箱。程序不自动执行生成的代码或 GPU 算子，运行前应独立审查。

## 报告问题

不要在公开 issue 中粘贴密钥、完整 `.env`、私人会话数据库或包含敏感数据的日志。可先提交不含利用细节和私密材料的问题摘要，请维护者提供私下沟通渠道。

若密钥已经外泄，先在供应商处撤销或轮换，再处理 Git 历史和其他副本；删除当前文件不能使旧密钥重新保密。

当前维护范围为 `main` 的最新代码，尚未承诺历史版本的安全修复周期。
